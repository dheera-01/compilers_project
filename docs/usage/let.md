# let statments 

Syntax:

```python
let <variable> = <value> (<body>);
```

Example:

```
assign a = let b = 10  (b ** 2);
print(a);
```

### Nested let

```
assign c = let a =9 (let b = 45 (a + b));
print(c);
```
